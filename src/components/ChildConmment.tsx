import React from "react";
import { View,Animated } from "react-native";
import { Text } from "react-native-paper";



export default function ChildComment({visible,id}: {visible: boolean,id: string}) {
    return (
        <></>
    )
}